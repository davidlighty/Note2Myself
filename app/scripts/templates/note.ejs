<section class="note-section">
    <div class="note-title">
        <h1 contenteditable="true" class="note-title"><%= title %></h1>
    </div>
    <div class="note-text">
        <p contenteditable="true" class="note-text">
        <%= text %>
        </p>
    </div>
</section>
<section class="note-extras">
    <ul>
        <!-- Created -->
        <!-- User -->
        <!-- Num of revision? -->
    </ul>
</section>
<section>
    <button class="update-note">Save</button>
    <button class="delete-note">X</button>
</section>